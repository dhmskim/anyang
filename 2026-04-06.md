# 2026-04-06 작업 내역

## 1. 보안 및 코드 품질 전면 개선

### 보안
- `.env` 환경변수 분리 (Ollama URL, 포트, JWT 시크릿, 관리자 계정, 공공데이터 키)
- `.gitignore`에 `.env` 추가, `.env.example` 제공
- 관리자 API에 JWT 인증 미들웨어 적용 (`authMiddleware` + `adminOnly`)
- 비밀번호 bcrypt 해시 저장 (평문 제거)
- sessionId를 `crypto.randomUUID()`로 변경 (예측 불가)
- Rate Limiting 추가 (API 60req/min, 로그인 20req/15min)
- Helmet 미들웨어 추가
- XSS 방어 - 사용자 입력 `textContent`/`escapeHtml()` 처리

### 데이터 관리
- JSON 파일 저장 시 tmp→rename 원자적 쓰기 + 잠금 처리
- 유저 데이터 서버 파일(`data/users.json`)에 영속 저장
- 예약 금액 서버에서 `discountId` 기반 직접 계산 (클라이언트 조작 불가)

### 코드 구조
- `script.js` (1,160줄) → 3개 모듈로 분할
  - `parking-portal/js/portal-ui.js` — 슬라이더, 탭, 조회, 주차현황, 지도
  - `parking-portal/js/auth.js` — 로그인/회원가입 (서버 API 연동)
  - `parking-portal/js/chatbot.js` — 챗봇 전체
- 전역 변수 제거 (IIFE 스코프 격리)
- inline `onclick` → `data-action` 이벤트 위임으로 변경
- `appendMsg` → `addMsg` 오타 수정

### 서버 API 추가
- `POST /api/auth/login` — JWT 토큰 발급
- `POST /api/auth/signup` — 회원가입 (bcrypt)
- `GET /api/auth/check-id/:id` — 아이디 중복확인
- `GET/POST/PUT/DELETE /api/admin/users` — 유저 CRUD (JWT 필수)
- `GET /api/geocode` — 주소→좌표 캐시 조회
- `GET /api/nearby-parking` — Overpass(OSM) 주변 주차장

## 2. 관리자 페이지 인증 연동
- JWT 자동 로그인 (localStorage 토큰 저장)
- 유저 관리 CRUD → 서버 API 연동 (영속성 확보)
- 대화 내역/상담 요청 API에 인증 헤더 추가

## 3. 지도 개선
- 위성 지도(Esri) + 라벨 오버레이 기본 적용
- 안양수목원 마커 (초록 나무 아이콘) 고정 표시
- 주차장 마커 호버 시 주차장명 툴팁 표시
- 마커 클릭 시 주차장명/주소/점유율 게이지/잔여면수 팝업
- 사이드바 목록 ↔ 지도 마커 양방향 연동
- Nominatim 지오코딩 → 서버 캐시(`data/geocode_cache.json`)로 좌표 저장

## 4. 공공데이터 API 연동 시도
- 전국주차장정보표준데이터 API 연동 시도
- 엔드포인트: `https://api.data.go.kr/openapi/tn_pubr_prkplce_info_api`
- 현재 상태: 승인 완료되었으나 키 활성화 미확인 (`SERVICE KEY IS NOT REGISTERED`)
- `.env`에 `PUBLIC_DATA_KEY` 추가 완료
- 키 활성화 확인 후 연동 예정

## 커밋
- 이전 커밋: `7d02ef9`

## TODO
- [ ] 공공데이터 API 키 활성화 확인 → 정확한 좌표 연동
- [ ] 또는 카카오 REST API로 주소→좌표 변환 대체
- [ ] Nominatim 중복 좌표 문제 해결
